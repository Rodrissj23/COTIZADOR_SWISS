const encoder=new TextEncoder();
const publicPaths=new Set(['/login','/login.html','/api/login','/api/logout','/.netlify/functions/login','/.netlify/functions/logout','/favicon.ico','/assets/images/login-doctor.jpg','/assets/images/swiss-medical-logo.png']);
const b64url=bytes=>{let binary='';for(const byte of bytes)binary+=String.fromCharCode(byte);return btoa(binary).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/g,'')};
const safeEqual=(a,b)=>{a=String(a??'');b=String(b??'');const len=Math.max(a.length,b.length,1);let diff=a.length^b.length;for(let i=0;i<len;i++)diff|=(a.charCodeAt(i%Math.max(a.length,1))||0)^(b.charCodeAt(i%Math.max(b.length,1))||0);return diff===0};
async function sign(value,secret){const key=await crypto.subtle.importKey('raw',encoder.encode(secret),{name:'HMAC',hash:'SHA-256'},false,['sign']);return b64url(new Uint8Array(await crypto.subtle.sign('HMAC',key,encoder.encode(value))))}
async function verify(token,secret){if(!token||!secret)return false;const parts=String(token).split('.');if(parts.length!==3||parts[0]!=='v1')return false;const expires=Number(parts[1]);if(!Number.isFinite(expires)||expires<=Math.floor(Date.now()/1000))return false;return safeEqual(parts[2],await sign(`${parts[0]}.${parts[1]}`,secret))}
function cookie(request,name){for(const part of(request.headers.get('cookie')||'').split(';')){const[key,...value]=part.trim().split('=');if(key===name)return decodeURIComponent(value.join('='))}return null}
export default async(request,context)=>{const url=new URL(request.url),path=url.pathname;if(publicPaths.has(path)||path.startsWith('/auth/')||path.startsWith('/css/'))return context.next();const secret=Netlify.env.get('SESSION_SECRET');if(!secret)return new Response('SESSION_SECRET no configurado.',{status:500,headers:{'Cache-Control':'no-store'}});if(await verify(cookie(request,'sm_session'),secret)){const response=await context.next();const headers=new Headers(response.headers);headers.set('Cache-Control','private, no-store');headers.delete('content-length');return new Response(response.body,{status:response.status,statusText:response.statusText,headers})}const login=new URL('/login.html',url.origin);login.searchParams.set('next',`${path}${url.search}`);return Response.redirect(login,302)};
