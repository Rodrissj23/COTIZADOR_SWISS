import fs from 'node:fs';
import vm from 'node:vm';
const root=new URL('../',import.meta.url);
const load=path=>vm.runInThisContext(fs.readFileSync(new URL(path,root),'utf8'),{filename:path});
globalThis.window={};
load('js/data-demo.js');load('js/tariff-audit-2026-09.js');load('js/tariff-sep26-update.js');load('js/quote-engine.js');
const plans=window.SWISS_PLANS;
const plan=name=>plans.find(p=>p.name===name);
const base=age=>({name:'QA',dni:'',zone:'AMBA',modality:'Directo',specialDiscount:'none',familyType:'individual',age,partnerAge:35,children:0,childrenAges:[],receiptContribution:0});
const q100=window.SWISS_ENGINE.familyQuote(plan('S2'),base(100));
if(q100.status!=='ok'||q100.listPrice!==730840)throw new Error(`S2 edad 100 no tomó banda Desde 61 Sep26: ${JSON.stringify(q100)}`);
const q100Partial=window.SWISS_ENGINE.familyQuote(plan('AMBU1'),base(100));
if(q100Partial.status!=='ok'||q100Partial.listPrice!==311363.5)throw new Error(`AMBU1 edad 100 no tomó Mayor de 80: ${JSON.stringify(q100Partial)}`);
console.log('PASS  Rangos finales abiertos: S2 actualizado Sep26 y AMBU1 sin cambios');
